export type eventType = {
  id: string;
  title: string;
  start: string;
  duration: string;
  end: string;
  left?: number;
  width?: number;
};

export type userType = {
  email: string;
  name: string;
};
